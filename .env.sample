# Environment variables for optional integrations
# Copy to .env and source in your shell init, or use a tool like direnv.

# AcoustID API key for audio fingerprint lookups (used by fix_track.py)
ACOUSTID_API_KEY=your_acoustid_api_key_here
